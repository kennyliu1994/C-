#ifndef __LIB_H__
#define __LIB_H__

#include <string>
#include <sstream>
#include <iostream> // cout
using namespace std;

class convert
{
public:
  convert(string input, int &output);
  /*convert(string, double &);
  convert(vector<double>, double &, int); //2's complement vector to decimal
  convert(double, vector<double> &);      //decimal to binary vector*/

private:
};

#endif //__LIB_H__